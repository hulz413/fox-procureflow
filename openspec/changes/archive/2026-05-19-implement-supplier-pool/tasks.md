## 1. 供应商池入口可达

- [x] 1.1 将侧边栏“供应商池”从静态项改为可点击导航入口，目标路由为 `/suppliers`，并保留数量提示和当前视觉风格
- [x] 1.2 为 `/suppliers` 添加显式 React Router 路由和工作台路由判断，确保该路径不会落到采购看板兜底视图
- [x] 1.3 更新中英文导航、标题、空态、筛选和详情抽屉文案，使供应商池页面在两种语言下都能独立理解

## 2. 真实供应商池列表

- [x] 2.1 新增供应商池页面视图，复用现有 master data context、supplier 和 category query，不使用前端静态 mock 数据填充列表
- [x] 2.2 在列表中展示 5 家 roadmap seed 供应商，并包含名称、服务范围、所在地、风险等级、状态和覆盖品类
- [x] 2.3 为后端加载中、错误和空结果提供清晰页面状态，避免后端不可用时显示伪造供应商数据

## 3. 供应商筛选与公司共享语境

- [x] 3.1 添加关键词、采购品类、风险等级和状态筛选控件，并让筛选结果基于后端 supplier 字段派生
- [x] 3.2 支持清空筛选恢复完整集团共享供应商池，并展示当前结果数量
- [x] 3.3 在供应商池页面展示当前公司语境或公司切换控件，验证切换两家演示公司后供应商池列表仍保持集团共享一致

## 4. 只读供应商详情

- [x] 4.1 支持从列表打开供应商详情抽屉，展示供应商基础信息、风险等级、状态、共享范围和覆盖品类
- [x] 4.2 在详情抽屉中展示集团共享供应商池与公司级采购交易隔离边界
- [x] 4.3 确认列表和详情抽屉不出现创建、编辑、删除、RFQ、PO、收货、发票、三单匹配、上传或下载等会改变状态的操作

## 5. 验证与收尾

- [x] 5.1 运行前端类型检查和构建，确认新增 `/suppliers` 页面不破坏现有工作台路由
- [x] 5.2 如实现扩展了 master data API 查询参数，补充后端 API 测试；如未扩展，则运行现有 master data API 测试确认供应商池仍返回 seeded 数据
- [x] 5.3 启动本地后端和前端，在浏览器验证从侧边栏进入 `/suppliers`、筛选供应商、打开详情抽屉和切换公司语境
- [x] 5.4 记录验证命令、浏览器 URL、关键供应商样例和任何环境 caveat，准备后续归档或同步 roadmap
