## 1. 搜索入口和业务范围

- [x] 1.1 将右上角搜索按钮定义为全局搜索入口，明确点击和 `Cmd/Ctrl + K` 打开同一个搜索弹窗
- [x] 1.2 梳理当前采购闭环可搜索字段，覆盖采购申请、审批、RFQ、采购订单、收货、发票、三单匹配、供应商池和核心主数据
- [x] 1.3 确认每类搜索结果的目标页面和详情定位参数，例如 `requestId`、`rfqId`、`poId`、`invoiceId`、`matchId`、`supplierId`

## 2. 后端全局搜索 API

- [x] 2.1 新增 global search DTO、结果类型枚举和统一响应结构，包含分组、上下文、命中字段和导航目标
- [x] 2.2 实现 `GET /api/global-search?companyId={companyId}&query={keyword}`，对空查询返回成功空结果
- [x] 2.3 聚合公司级交易数据搜索，确保采购申请、审批、RFQ、PO、收货、发票和三单匹配都按 `companyId` 隔离
- [x] 2.4 聚合集团共享和公司级主数据搜索，确保供应商池和采购品类可共享，部门、用户、预算科目按公司归属返回
- [x] 2.5 实现结果排序和数量限制，让精确编号或强前缀命中优先于标题、供应商和说明类命中
- [x] 2.6 为 global search endpoint 添加 OpenAPI 注解和错误响应说明

## 3. 前端搜索弹窗体验

- [x] 3.1 将 topbar 搜索按钮接入 command-palette 式弹窗，并保持当前工作台 shell 和 topbar 布局稳定
- [x] 3.2 实现搜索输入、加载、错误、空结果和分组结果渲染，所有结果来自后端 API 而非静态 mock
- [x] 3.3 使用当前公司语境调用搜索 API，并在结果中展示公司、共享范围、状态、供应商、金额或来源单据等上下文
- [x] 3.4 支持键盘上下选择、Enter 打开结果、Esc 关闭弹窗，选中项需要有清晰视觉状态
- [x] 3.5 为搜索弹窗补充中文和英文文案，包括占位符、空态、错误态、结果类型和共享/公司隔离提示

## 4. 搜索结果跳转和详情定位

- [x] 4.1 支持采购申请搜索结果跳转 `/purchase-requests` 并打开或选中目标申请详情
- [x] 4.2 支持审批搜索结果跳转 `/approvals` 并打开或选中目标审批详情
- [x] 4.3 支持 RFQ 和采购订单搜索结果跳转 `/rfqs`、`/purchase-orders` 并打开或选中目标详情
- [x] 4.4 支持收货、发票和 PO 履约搜索结果跳转 `/receipts-invoices` 并定位相关 PO 或单据详情
- [x] 4.5 支持三单匹配搜索结果跳转 `/three-way-matching` 并打开或选中目标匹配详情
- [x] 4.6 支持供应商和主数据搜索结果跳转 `/suppliers`、`/master-data`，供应商结果打开只读详情

## 5. 公司隔离和只读验证

- [x] 5.1 增加后端集成测试，验证 `company-digital` 搜索不会返回 `company-manufacturing` 的交易记录
- [x] 5.2 增加后端测试，验证集团共享供应商池可以在当前公司搜索结果中出现并标记共享范围
- [x] 5.3 增加后端测试，验证未知 `companyId` 被拒绝，空查询返回成功空结果
- [x] 5.4 增加前端测试，验证搜索弹窗打开、输入、结果展示、键盘选择和结果跳转行为
- [x] 5.5 确认搜索行为不会触发创建、审批、发布、收货、开票、匹配处理、附件上传或删除等业务状态变更

## 6. 演示和回归

- [x] 6.1 用真实 seeded 数据验证搜索 `PR`、`RFQ`、`PO`、供应商名称、发票号和三单匹配异常线索
- [x] 6.2 验证星河数字科技有限公司和星河智能制造有限公司公司语境下的交易搜索结果不同，供应商池结果保持集团共享
- [x] 6.3 运行前端 lint/build 和相关测试，使用满足 Vite 要求的 Node.js 版本
- [x] 6.4 运行相关后端测试或 smoke check，确认新增搜索 API 不影响现有采购闭环页面
